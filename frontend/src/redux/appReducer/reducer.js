

const init={
    data:[],
    isLoading:false,
    isError:false
}

const reducer=(state=init,{type,payload})=>{

    switch(type){

        default:
            return state
    }
}

export{reducer}